services:
  - type: web
    name: 판례요약서비스
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_summarizer:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false  # Render 대시보드에서 수동 설정
